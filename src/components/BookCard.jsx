export function BookCard(name) {

    return(
        <>

            <div style={{border: "solid", borderRadius: "10px", borderColor: "white"}}>
                <h2 style={{padding: "15px"}}>{name}</h2>
            </div>

        </>
    )

}